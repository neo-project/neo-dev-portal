import React from 'react';
import Layout from '@theme/Layout';

export default function WalletConnect() {
    return (
        <Layout>
            <div className="twcontainer mx-auto">
                <h1>WalletConnect</h1>
                <button>Connect wallet</button>
            </div>
        </Layout>
    );
}